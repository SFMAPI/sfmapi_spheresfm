[project]
name = "sfmapi-spheresfm"
version = "0.0.1"
description = "sfmapi backend-action wrapper for SphereSfM"
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.12,<3.13"
dependencies = [
    "sfmapi>=0.0.1,<0.1",
]

[project.optional-dependencies]
server = []
dev = [
    "pytest>=8.3",
    "ruff>=0.8",
]
mcp = [
    "fastmcp==3.2.4",
]

[project.scripts]
sfmapi-spheresfm-api = "sfmapi_spheresfm.api_launcher:main"
sfmapi-spheresfm-info = "sfmapi_spheresfm.cli:main"

[project.entry-points."sfmapi.backends"]
spheresfm = "sfmapi_spheresfm.plugin:plugin"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/sfmapi_spheresfm"]

[tool.uv.sources]
sfmapi = { path = "../sfmapi", editable = true }

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM", "PT", "RUF"]
ignore = ["E501"]
